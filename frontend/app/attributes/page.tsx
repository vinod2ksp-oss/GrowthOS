"use client";
import { useEffect,useState } from 'react';import { apiRequest } from '@/lib/api';import type { Goal } from '@/types';
type Attribute={attribute_key:string;current_status:string;range_min:number|null;range_max:number|null;confidence:number;evidence_count:number;explanation:string;gap_status:string;target_range:{min:number|null;max:number|null;unit:string|null}|null};
export default function AttributesPage(){const [items,setItems]=useState<Attribute[]>([]);const[goals,setGoals]=useState<Goal[]>([]);const[goalId,setGoalId]=useState('');const[goalsLoaded,setGoalsLoaded]=useState(false);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{apiRequest<Goal[]>('/goals').then(data=>{setGoals(data);setGoalId(data[0]?.id||'')}).catch(e=>setError(e.message)).finally(()=>setGoalsLoaded(true));},[]);useEffect(()=>{if(!goalsLoaded)return;setLoading(true);apiRequest<Attribute[]>(`/growth-attributes${goalId?`?goal_id=${goalId}`:''}`).then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false));},[goalId,goalsLoaded]);return <main className="mx-auto max-w-6xl p-6"><h1 className="text-2xl font-bold">能力面板</h1>{goals.length>0&&<select aria-label="能力目标" className="mt-4 border px-3 py-2" value={goalId} onChange={e=>setGoalId(e.target.value)}>{goals.map(goal=><option key={goal.id} value={goal.id}>{goal.target_major||goal.target_school||'未命名目标'}</option>)}</select>}{loading?<p>加载中...</p>:error?<p className="text-red-600">{error}</p>:<div className="mt-4 grid gap-3 md:grid-cols-2">{items.map(item=><article key={item.attribute_key} className="border p-4"><h2 className="font-semibold">{item.attribute_key}</h2><p>状态：{item.current_status}</p><p>评估区间：{item.range_min===null?'证据不足':`${(item.range_min*100).toFixed(1)}% - ${((item.range_max||item.range_min)*100).toFixed(1)}%`}</p><p>目标区间：{item.target_range?.min===null||!item.target_range?'未确认':`${item.target_range.min} - ${item.target_range.max??'未设上限'} ${item.target_range.unit||''}`}</p><p>差距状态：{item.gap_status}</p><p>可信度：{Math.round(item.confidence*100)}% / 证据 {item.evidence_count} 条</p><p className="mt-2 text-sm text-gray-600">{item.explanation}</p></article>)}</div>}</main>}
