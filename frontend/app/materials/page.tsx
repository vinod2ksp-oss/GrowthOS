"use client";
import { useEffect, useState } from 'react';
import { apiRequest } from '@/lib/api';

type Material = { id: string; original_filename: string; material_type: string; parse_status: string; confirmation_status: string; parsed_content?: { extracted_text?: string | null; requires_manual_entry?: boolean } };

export default function MaterialsPage() {
  const [items, setItems] = useState<Material[]>([]); const [selected, setSelected] = useState<Material | null>(null);
  const [file, setFile] = useState<File | null>(null); const [type, setType] = useState('transcript');
  const [course, setCourse] = useState({ course_name: '', course_category: 'mathematical_foundation', score: '', full_score: '', credit: '', semester: '', is_core: false });
  const [entryMode, setEntryMode] = useState<'course'|'experience'>('course');
  const [experience, setExperience] = useState({ experience_type: 'project', name: '', organization: '', start_date: '', end_date: '', description: '', outcome: '', associated_skills: [] as string[] });
  const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [message, setMessage] = useState('');
  async function load() { setLoading(true); try { setItems(await apiRequest<Material[]>('/materials')); } catch (e) { setError(e instanceof Error ? e.message : '加载失败'); } finally { setLoading(false); } }
  useEffect(() => { load(); }, []);
  async function upload() { if (!file) return; setError(''); const body = new FormData(); body.set('material_type', type); body.set('file', file); try { const value = await apiRequest<Material>('/materials', { method: 'POST', body }); setItems(current => [value, ...current]); setSelected(value); setMessage('材料已上传，请确认结构化结果'); } catch (e) { setError(e instanceof Error ? e.message : '上传失败'); } }
  async function confirm() { if (!selected || (entryMode==='course'?!course.course_name:!experience.name)) return; const payload=entryMode==='course'?{ courses: [{ ...course, score: Number(course.score), full_score: Number(course.full_score), credit: course.credit ? Number(course.credit) : null }], experiences: [] }:{courses:[],experiences:[{...experience,start_date:experience.start_date||null,end_date:experience.end_date||null}]}; try { await apiRequest(`/materials/${selected.id}/confirm`, { method: 'POST', body: JSON.stringify(payload) }); setMessage('解析结果已确认并生成成长证据'); await load(); } catch (e) { setError(e instanceof Error ? e.message : '确认失败'); } }
  async function remove(){if(!selected)return;try{await apiRequest(`/materials/${selected.id}`,{method:'DELETE'});setSelected(null);setMessage('材料及其关联结果已删除');await load()}catch(e){setError(e instanceof Error?e.message:'删除失败')}}
  return <main className="mx-auto max-w-6xl p-6"><h1 className="text-2xl font-bold">材料中心</h1>{error && <p className="mt-3 text-red-600">{error}</p>}{message && <p className="mt-3 text-green-700">{message}</p>}
    <section className="mt-5 grid gap-3 border-b pb-5 md:grid-cols-[180px_1fr_auto]"><select aria-label="材料类型" className="border px-3 py-2" value={type} onChange={e => setType(e.target.value)}><option value="transcript">成绩单</option><option value="resume">简历</option><option value="course_grade">课程成绩截图</option><option value="research">论文或录用证明</option><option value="internship">实习证明</option><option value="competition">竞赛证书</option><option value="project">项目成果</option><option value="other">其他材料</option></select><input aria-label="选择材料" type="file" accept=".pdf,.docx,.png,.jpg,.jpeg" onChange={e => setFile(e.target.files?.[0] || null)} /><button className="border bg-slate-900 px-4 py-2 text-white" onClick={upload} disabled={!file}>上传材料</button></section>
    <div className="mt-5 grid gap-6 lg:grid-cols-2"><section><h2 className="font-semibold">已上传材料</h2>{loading ? <p>加载中...</p> : items.length === 0 ? <p className="text-gray-600">尚未上传材料。</p> : items.map(item => <button key={item.id} className="mt-2 block w-full border p-3 text-left" onClick={() => setSelected(item)}><b>{item.original_filename}</b><span className="block text-sm text-gray-600">解析：{item.parse_status} / 确认：{item.confirmation_status}</span></button>)}</section>
    <section><h2 className="font-semibold">解析结果确认</h2>{!selected ? <p className="text-gray-600">请选择一份材料。</p> : <div className="mt-2 grid gap-2"><p className="text-sm">提取文本：{selected.parsed_content?.extracted_text || '待人工补充'}</p><select aria-label="录入类型" className="border px-3 py-2" value={entryMode} onChange={e=>setEntryMode(e.target.value as 'course'|'experience')}><option value="course">课程成绩</option><option value="experience">简历或经历</option></select>{entryMode==='course'?<><input className="border px-3 py-2" placeholder="课程名称" value={course.course_name} onChange={e => setCourse({...course, course_name:e.target.value})}/><select aria-label="课程类别" className="border px-3 py-2" value={course.course_category} onChange={e => setCourse({...course, course_category:e.target.value})}><option value="mathematical_foundation">数学基础</option><option value="english">英语</option><option value="professional_knowledge">专业知识</option><option value="programming_tools">编程工具</option><option value="data_analysis">数据分析</option></select><input className="border px-3 py-2" placeholder="成绩" type="number" value={course.score} onChange={e => setCourse({...course, score:e.target.value})}/><input className="border px-3 py-2" placeholder="满分" type="number" value={course.full_score} onChange={e => setCourse({...course, full_score:e.target.value})}/><input className="border px-3 py-2" placeholder="学分（可选）" type="number" value={course.credit} onChange={e => setCourse({...course, credit:e.target.value})}/><input className="border px-3 py-2" placeholder="学期（可选）" value={course.semester} onChange={e => setCourse({...course, semester:e.target.value})}/><label><input type="checkbox" checked={course.is_core} onChange={e => setCourse({...course, is_core:e.target.checked})}/> 核心课程</label></>:<><select aria-label="经历类型" className="border px-3 py-2" value={experience.experience_type} onChange={e=>setExperience({...experience,experience_type:e.target.value})}><option value="research">科研</option><option value="internship">实习</option><option value="project">项目</option><option value="competition">竞赛</option><option value="certificate">证书</option><option value="portfolio">作品集</option><option value="other">其他</option></select><input className="border px-3 py-2" placeholder="经历名称" value={experience.name} onChange={e=>setExperience({...experience,name:e.target.value})}/><input className="border px-3 py-2" placeholder="组织或单位" value={experience.organization} onChange={e=>setExperience({...experience,organization:e.target.value})}/><input className="border px-3 py-2" type="date" aria-label="开始时间" value={experience.start_date} onChange={e=>setExperience({...experience,start_date:e.target.value})}/><input className="border px-3 py-2" type="date" aria-label="结束时间" value={experience.end_date} onChange={e=>setExperience({...experience,end_date:e.target.value})}/><textarea className="border px-3 py-2" placeholder="经历描述" value={experience.description} onChange={e=>setExperience({...experience,description:e.target.value})}/><textarea className="border px-3 py-2" placeholder="成果" value={experience.outcome} onChange={e=>setExperience({...experience,outcome:e.target.value})}/></>}<button className="border bg-slate-900 px-4 py-2 text-white" onClick={confirm}>确认结构化结果</button><button className="border px-4 py-2 text-red-700" onClick={remove}>删除本人材料</button></div>}</section></div></main>;
}
